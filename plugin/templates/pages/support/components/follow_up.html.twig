{% import 'components/form/fields_macros.html.twig' as fields %}
{% import '@iservice/components/fields_macros.html.twig' as fieldsIservice %}

{{ fields.textareaField(
    '_followup_content',
    item.fields['_followup[content]'],
    _t('Followup description'),
    field_options|merge({
        'enable_richtext': 1,
        'enable_images': 0,
    })
) }}

{{ fields.checkboxField(
    '_followup[is_private]]',
    item.fields['_followup[is_private]'],
    _t('Private followup'),
    field_options
) }}

{% if followupsData %}
    {{ fieldsIservice.bootstrapGridTableField(
        _n('Followup', 'Followups', 2),
        followupsData
    ) }}
{% endif %}
